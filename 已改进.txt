增加IE、FireFox、Chrome、Safari、支持
使用CometContext类接收初始化配置，去掉Config，构造Connector,Messenger等，并在初始化完毕后调用CometContextListennerInterface
将Connector与Messenger由单态变为一般的类，有以便使用者进行扩展
增加发送失败缓存(BadCache），防止信息丢失
去除Beat机制，变为timeout续连机制，一可解决客户端雪球，二可与longpooling兼容，三配合BadCache可解决信息丢失
可自动选取longpooling,httpstreaming,auto(自动选择工作模式来兼容所有浏览器)
连接后立即给出建立成功回应，并有可能将一些必要的服务器端配置信息一并带回(服务器配置（连接方式，连接超时间,debug，服务器版本），用户配置，离线信息)
重新组织可被客户端感知的Event，EventType由常量变为自动取类名
增加必要的服务器端事件，有助于开发人员扩展和管理
增加了多条消息一同发给同一个连接功能(需改客户端
客户端连接时需携带客户引擎版本号
考虑是否增加服务器端事件管理器，用于注册所有服务器端事件

提供一个javaClinetEngine类，供远程(或本地)java客户端响应事件使用，提供一个
考虑增加连接方式（longpooling,httpstreaming,auto）、开启Debug、重连超时时间(回收时判断超过多久的连接需要清除)、BadCache回收时间
需要一个json序列化与反序列化的通用类
服务器端事件机制有待于改进

在Connector上增加一个监视线程，监视并删除那些没有response且超时的连接

增加了应用模块扩展机制
解决了IE6下无法正常运行的问题